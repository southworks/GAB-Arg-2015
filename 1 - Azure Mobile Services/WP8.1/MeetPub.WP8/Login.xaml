<Page
    x:Class="MeetPub.WP8.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarMeetUp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <ImageBrush ImageSource="Assets/home_background.png"></ImageBrush>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <Button x:Name="LoginFacebookButton" VerticalAlignment="Bottom" Margin="25 15" Background="#5F4B3C" HorizontalAlignment="Stretch" Click="LoginFacebookClick" BorderThickness="2">LOGIN WITH FACEBOOK</Button>
            
        </Grid>
        <ProgressRing Grid.Row="0" Grid.RowSpan="2" x:Name="LoginProgress"></ProgressRing>
    </Grid>
</Page>
